import 'package:flutter/material.dart';

class SingleEventDay extends StatelessWidget {
  const SingleEventDay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
